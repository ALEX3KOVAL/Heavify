import{Router}from"express";import {AlbumController} from"../../api/controllers/api.controllers.min.js";const albumRouter=Router();albumRouter.post("/",AlbumController.add),albumRouter.get("/",AlbumController.getAll),albumRouter.get("/:id",AlbumController.getOne);import {AuthorController} from"../../api/controllers/api.controllers.min.js";const authorRouter=Router();authorRouter.post("/",AuthorController.add),authorRouter.get("/",AuthorController.getAll);const genreRouter=Router();import {PictureController} from"../../api/controllers/api.controllers.min.js";const pictureRouter=Router();pictureRouter.get("/:pageName/:componentType/",PictureController.getFoldersNamesBy),pictureRouter.get("/:pageName/:componentType/:componentName?",PictureController.getPicturesGroupByNames);import {SelectionController} from"../../api/controllers/api.controllers.min.js";const selectionRouter=Router();selectionRouter.get("/",SelectionController.getAll);import {SongController} from"../../api/controllers/api.controllers.min.js";import{checkRoleMiddleware,authMiddleware}from"../../middleware/middlewares.min.js";const songRouter=Router();songRouter.post("/",authMiddleware,checkRoleMiddleware("ADMIN"),SongController.add),songRouter.get("/",authMiddleware,SongController.getAll),songRouter.get("/:id",authMiddleware,SongController.getOne);import {UserController} from"../../api/controllers/api.controllers.min.js";import{activateMiddleware}from"../../middleware/middlewares.min.js";const userRouter=Router();userRouter.post("/registration",UserController.registration),userRouter.post("/login",activateMiddleware,UserController.login),userRouter.get("/activate/:link",UserController.activate),userRouter.post("/logout",UserController.logout),userRouter.get("/refresh",UserController.refresh);export{albumRouter,authorRouter,genreRouter,pictureRouter,selectionRouter,songRouter,userRouter};
