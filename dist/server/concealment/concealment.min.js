import*as crypto from"crypto";const key=crypto.randomBytes(Number(process.env.AES_SECRET_KEY)),algorithm=process.env.ALGORITHM,initVector=crypto.randomBytes(Number(process.env.AES_IV)),cipher=crypto.createCipheriv(algorithm,key,initVector,{}),decipher=crypto.createDecipheriv(algorithm,key,initVector),encryptValue=e=>{e=cipher.update(e,"utf-8","hex");return e+=cipher.final("hex")},decryptValue=e=>{decipher.setAutoPadding(!1);e=decipher.update(e,"hex","utf-8");return e+=decipher.final("utf8")};export{encryptValue,decryptValue};